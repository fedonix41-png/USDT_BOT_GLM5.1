# План: Исправление IDOR и добавление логирования атак

## Задачи

1. **Исправить IDOR-уязвимость** в `broken_link.py` — добавить проверку владельца заказа
2. **Добавить логирование** попыток несанкционированного доступа во всех защищённых хендлерах

---

## Изменяемые файлы

### 1. `app/handlers/common/broken_link.py`
**Задача:** Добавить проверку `order.user_id == user.id`

**Изменения:**
- Добавить импорт `User` и `RoleEnum`
- Добавить параметр `user: User | None` в сигнатуру хендлера
- Добавить проверку:
  ```python
  if user is None:
      await callback.answer("Ошибка: пользователь не найден.", show_alert=True)
      return

  if order.user_id != user.id:
      logger.warning(f"Unauthorized broken_link attempt: user_id={callback.from_user.id}, order_id={order_id}, owner_id={order.user_id}")
      await callback.answer("Это не ваша заявка.", show_alert=True)
      return
  ```

---

### 2. Добавить логирование во всех защищённых хендлерах

#### `app/handlers/operator/complete_order.py`
- Строка 25-27: добавить `logger.warning()` после проверки прав
- Строка 76-78: добавить `logger.warning()` после проверки прав

```python
if user is None or user.role not in (RoleEnum.operator, RoleEnum.admin, RoleEnum.super_admin):
    logger.warning(f"Unauthorized access attempt: user_id={callback.from_user.id}, callback={callback.data}, required_role=operator+")
    await callback.answer("У вас недостаточно прав.", show_alert=True)
    return
```

#### `app/handlers/operator/active_orders.py`
- Строка 25-26: добавить `logger.warning()` для message handler
- Строка 52-54: добавить `logger.warning()` для callback handler

#### `app/handlers/admin/management.py`
Все проверки в callbacks:
- `toggle_buy` (строка 64-66)
- `toggle_sell` (строка 82-84)
- `toggle_bot` (строка 100-102)
- `start_change_buy_rate` (строка 123-125)
- `start_change_sell_rate` (строка 142-144)
- `start_change_links` (строка 161-163)
- `start_notification_chats` (строка 179-181)
- `start_assign_operator` (строка 192-194)
- `start_assign_admin` (строка 207-209) — здесь только super_admin

#### `app/handlers/admin/notification_chats.py`
- `list_chats` (строка 31-33)
- `start_add_chat` (строка 51-53)
- `process_add_chat` (строка 88-91)
- `start_delete_chat` (строка 119-121)
- `delete_chat` (строка 138-140)

#### `app/handlers/admin/change_rate.py`
- `process_new_buy_rate` (строка 31-34)
- `process_new_sell_rate` (строка 74-77)

#### `app/handlers/admin/change_links.py`
- `choose_link_type` (строка 33-35)
- `process_new_link` (строка 51-54)

#### `app/handlers/admin/assign_roles.py`
- `process_assign_operator` (строка 50-53)
- `process_assign_admin` (строка 103-106)

#### `app/handlers/operator/statistics.py`
- `start_statistics` (строка 29-30) — добавить logger.warning()

---

### 3. Обновить документацию `docs/risks.md`
Добавить информацию о:
- Исправленной IDOR в `broken_link.py`
- Внедрённом логировании попыток атак

---

## Шаблон логирования

```python
# Для callback handlers:
logger.warning(
    f"Unauthorized access attempt: user_id={callback.from_user.id}, "
    f"callback={callback.data}, required_role={required_roles}"
)

# Для message handlers:
logger.warning(
    f"Unauthorized access attempt: user_id={message.from_user.id}, "
    f"command={message.text}, required_role={required_roles}"
)
```

---

## Порядок выполнения

1. Исправить `broken_link.py` (IDOR)
2. Добавить логирование в `complete_order.py`
3. Добавить логирование в `active_orders.py`
4. Добавить логирование в `management.py`
5. Добавить логирование в `notification_chats.py`
6. Добавить логирование в `change_rate.py`
7. Добавить логирование в `change_links.py`
8. Добавить логирование в `assign_roles.py`
9. Добавить логирование в `statistics.py`
10. Обновить `docs/risks.md`

---

## Риски

- **Низкий**: Изменения затрагивают только проверки прав и логирование, бизнес-логика не меняется
- **Тестирование**: Проверить что логи появляются при попытке доступа без прав
